const fonts = {
  body:
    'system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", sans-serif',
  heading:
    'system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", sans-serif',
  monospace: "'JetBrains Mono', Menlo, Monaco, monospace",
}

const fontSizes = [12, 14, 16, 20, 24, 32, 48, 64, 96]

const fontWeights = {
  body: 400,
  heading: 700,
  bold: 700,
}

module.exports = { fontSizes, fonts, fontWeights }
